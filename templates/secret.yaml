# {{- if not .Values.mongodb.auth.existingSecret -}}

# apiVersion: v1
# kind: Secret
# metadata:
#   name: {{ .Values.secret.name }}
# type: Opaque
# data:
#   mongodb-usernames: {{ .Values.mongodb.auth.userNames[0]  | b64enc }}
#   mongodb-passwords: {{ .Values.mongodb.auth.passwords[0] | b64enc }}
  
# {{- end -}}